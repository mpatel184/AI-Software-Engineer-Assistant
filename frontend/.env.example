# Base path the browser uses for API calls. Defaults to the Next.js rewrite proxy.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Where the Next.js rewrite proxies /api/* (server-side). In Docker: http://api:8000
API_PROXY_URL=http://localhost:8000
